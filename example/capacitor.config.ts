import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.plugin.deviceorientation',
  appName: 'example',
  webDir: 'www',
};

export default config;
